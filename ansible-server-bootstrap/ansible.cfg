# Disabling Host Key Checking
[defaults]
host_key_checking = False
inventory = inventories/dev/hosts.ini
remote_user = devops
interpreter_python = auto_silent
private_key_file = ~/.ssh/devops-lab-key.pem
vault_password_file = vault_password_script.py
